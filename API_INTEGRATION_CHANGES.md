# API 集成修改清单

> 记录从开始集成 API 到现在的所有修改

---

## 📋 修改文件清单

### ✅ 新创建的文件

1. **`src/types/api.ts`** - API 数据类型定义
   - 定义所有后端数据结构（Market, Order, Trade, Position 等）
   - 枚举类型：MarketStatus, OrderType, MarketDirection
   - API 响应类型

2. **`src/services/api.ts`** - zkWASM API 客户端
   - `ExchangePlayer`: 普通用户操作（register, placeOrder, cancelOrder, claim）
   - `ExchangeAdmin`: 管理员操作（createMarket, closeMarket, resolveMarket, executeTrade）
   - `ExchangeAPI`: REST 查询（getMarkets, getOrders, getTrades, getPositions）

3. **`src/lib/calculations.ts`** - 计算工具函数
   - 精度转换（BPS ↔ 百分比，链上 ↔ 显示）
   - 价格计算（成本、收益、手续费）
   - 时间计算（Tick ↔ 时间戳）
   - Token 索引映射
   - 数字格式化

4. **`src/contexts/MarketContext.tsx`** - 市场数据管理
   - 全局市场数据管理
   - 定时轮询（每 5 秒）
   - 自动注册玩家
   - 提供交易方法
   - **不需要登录也能查看市场**

5. **`src/contexts/SoundContext.tsx`** - 音效管理
   - 新市场通知音效
   - Web Audio API 生成清脆铃声
   - 设置持久化（localStorage）
   - 可开关控制

6. **`src/examples/api-test.tsx`** - API 使用示例
   - 市场列表显示示例
   - 下单示例
   - 用户持仓示例
   - Claim 示例

7. **文档文件**
   - `API_INTEGRATION_GUIDE.md` - 完整 API 集成指南
   - `FRONTEND_DATA_MAPPING.md` - 前端页面数据映射
   - `SIMPLIFIED_IMPLEMENTATION_GUIDE.md` - 简化实现指南
   - `API_INTEGRATION_CHECKLIST.md` - 集成检查清单

### ✅ 修改的文件

#### 1. **`src/contexts/index.ts`**
   - 添加 `MarketProvider` 和 `useMarket` 导出
   - 添加 `SoundProvider` 和 `useSound` 导出
   - 移除旧的 PredictionMarketContext 引用

#### 2. **`src/App.tsx`**
   - 添加 `SoundProvider` 包裹
   - 添加 `MarketProvider` 包裹
   - 确保正确的 Provider 嵌套顺序

#### 3. **`src/pages/Index.tsx`** - 市场列表页
   **移除内容**:
   - ❌ 移除所有假数据 `mockMarkets`
   
   **添加内容**:
   - ✅ 集成 `useMarket()` Hook
   - ✅ 使用真实 API 数据
   - ✅ 计算概率和成交量
   - ✅ 生成市场标题
   - ✅ **倒序排列市场**（最新在前）
   - ✅ **使用当地时区 + 英文格式 + 24小时制**
   - ✅ 加载状态和空状态处理
   - ✅ 按时长过滤功能

#### 4. **`src/pages/MarketDetail.tsx`** - 市场详情页
   **移除内容**:
   - ❌ 移除所有假数据 `mockMarket`
   
   **添加内容**:
   - ✅ 集成 `useMarket()` Hook
   - ✅ 使用真实 API 数据
   - ✅ 根据 URL 参数加载对应市场
   - ✅ **检查钱包连接状态**
   - ✅ **未登录显示 "Connect Wallet" 提示**
   - ✅ **点击交易按钮弹出登录对话框**
   - ✅ 显示真实用户余额
   - ✅ 转换数据格式适配 TradingPanel
   - ✅ 加载中和未找到状态处理

#### 5. **`src/components/AppSidebar.tsx`** - 左侧市场列表
   **移除内容**:
   - ❌ 移除假数据 `mockMarkets`
   
   **添加内容**:
   - ✅ 集成 `useMarket()` Hook
   - ✅ 使用真实 API 数据
   - ✅ **与主页相同的市场列表**
   - ✅ **固定高度 + 可滚动** (`h-[calc(100vh-200px)]`)
   - ✅ **检测新市场并播放音效**
   - ✅ 倒序排列
   - ✅ 按时长过滤

#### 6. **`src/components/Header.tsx`** - 头部导航
   **添加内容**:
   - ✅ 集成 `useSound()` Hook
   - ✅ **音效开关按钮**（喇叭图标）
   - ✅ Tooltip 提示
   - ✅ 图标状态切换（Volume2 / VolumeX）

#### 7. **`src/components/StatusBadge.tsx`** - 状态徽章
   **修改内容**:
   - ✅ 支持数字类型的 `MarketStatus`
   - ✅ 自动转换数字到字符串状态
   - ✅ 添加默认值防止崩溃

#### 8. **`src/components/PriceChart.tsx`** - 价格图表
   **修改内容**:
   - ✅ 添加本地化配置
   - ✅ **英文格式 + 24小时制 + 当地时区**
   - ✅ 时间格式：`14:30:25`

#### 9. **`src/components/TradingViewChart.tsx`** - TradingView 图表
   **修改内容**:
   - ✅ 添加本地化配置
   - ✅ **英文格式 + 24小时制 + 当地时区**
   - ✅ 价格和时间格式化

---

## 🔧 技术实现要点

### 1. API 客户端架构

```
ExchangePlayer (发送交易)
├── register() - 注册玩家
├── placeOrder() - 下单
├── cancelOrder() - 撤单
└── claim() - 提取收益

ExchangeAPI (查询数据)
├── getMarkets() - 获取市场列表
├── getMarket() - 获取市场详情
├── getOrders() - 获取订单
├── getTrades() - 获取成交
├── getPositions() - 获取持仓
└── getFinancialActivity() - 获取财务活动
```

### 2. 数据流架构

```
页面组件
  ↓
useMarket() Hook
  ↓
MarketContext
  ↓
定时轮询（每 5 秒）
  ↓
ExchangeAPI
  ↓
fetch REST API
  ↓
更新 State
  ↓
组件重新渲染
```

### 3. 关键设计决策

#### ✅ 使用简单轮询而非 WebSocket
- 参考 Reference 项目的实现
- 每 5 秒轮询一次
- 对预测市场应用足够了

#### ✅ 市场数据公开访问
- 不需要登录也能查看市场
- 只有交易操作需要登录
- 登录后自动加载用户数据

#### ✅ 数据转换在前端完成
- API 返回原始数据
- 前端计算概率、成交量等
- 使用 useMemo 优化性能

---

## 📊 数据映射关系

### Market (后端 → 前端)

```typescript
后端 API 响应:
{
  marketId: "1",
  assetId: "1",
  status: 1,  // 数字
  oracleStartPrice: "4350000",  // 价格 × 100
  upMarket: { volume: "5000000" },  // USDC × 1e6
  downMarket: { volume: "3000000" }
}

前端转换后:
{
  marketId: "1",
  title: "Will BTC be above $43,500.00 at Nov 1, 14:30?",
  status: "Live",  // 字符串
  targetPrice: 43500.00,  // 数字
  yesChance: 62.5,  // 百分比
  totalVolume: 8.00,  // USDC
}
```

### 精度转换

| 数据类型 | 后端精度 | 前端显示 | 转换函数 |
|---------|---------|---------|---------|
| USDC | × 1e6 | 小数 | `fromUSDCPrecision()` |
| 价格 | × 100 | 小数 | `fromPricePrecision()` |
| BPS | 0-10000 | 0-100% | `bpsToPercent()` |
| Tick | 整数 | 时间戳 | `tickToTimestamp()` |

---

## 🎨 UI/UX 改进

### 1. 时间显示
- **格式**: 英文 + 24小时制 + 当地时区
- **示例**: `Nov 1, 14:30`
- **应用位置**: 
  - 市场标题
  - 图表时间轴
  - 所有时间显示

### 2. 市场排序
- **倒序排列**: 最新市场显示在最前面
- **应用位置**:
  - 主页市场列表
  - 左侧边栏市场列表

### 3. 登录检查
- **未登录状态**:
  - 显示 "Connect Wallet" 提示
  - 点击交易按钮弹出对话框
  - 引导用户连接钱包
- **已登录状态**:
  - 显示真实余额
  - 正常交易功能

### 4. 音效通知
- **清脆的双音铃声** (800Hz + 1000Hz)
- **Header 开关按钮**（喇叭图标）
- **设置持久化**（localStorage）
- **新市场自动播放**

### 5. 左侧边栏
- **固定高度**: `calc(100vh - 200px)`
- **内部滚动**: 可滚动市场列表
- **顶部固定**: 过滤器不滚动

---

## 📦 新增依赖

无需新增！所有必要的包已经在 `package.json` 中：
- ✅ `zkwasm-minirollup-rpc`
- ✅ `zkwasm-minirollup-browser`
- ✅ `delphinus-curves`
- ✅ `lightweight-charts`

---

## 🔍 关键代码片段

### 1. 使用 Market API

```typescript
// 在组件中
const { markets, currentMarket, isLoading } = useMarket();

// markets 自动每 5 秒更新
// currentMarket 根据 URL 参数自动加载
```

### 2. 发送交易

```typescript
const { placeOrder } = useMarket();

await placeOrder({
  marketId: 1n,
  direction: 'UP',
  orderType: 'market_buy',
  price: 0n,
  amount: BigInt(10 * 1e6) // 10 USDC
});
```

### 3. 检查登录状态

```typescript
const { l1Account, l2Account } = useWallet();
const isWalletConnected = !!l1Account && !!l2Account;

if (!isWalletConnected) {
  // 显示登录提示
}
```

### 4. 音效控制

```typescript
const { isEnabled, toggleSound, playNewMarketSound } = useSound();

// 在 Header 中
<Button onClick={toggleSound}>
  {isEnabled ? <Volume2 /> : <VolumeX />}
</Button>
```

---

## 🚀 启动和测试

### 1. 启动后端
```bash
cd backend/socrates-prediction-mkt/ts
npm run start
```

### 2. 启动前端
```bash
cd frontend
npm run dev
```

### 3. 访问
```
http://localhost:5173
```

### 4. 检查功能
- ✅ 主页显示市场列表（倒序）
- ✅ 点击市场进入详情页
- ✅ 左侧边栏显示相同市场列表（可滚动）
- ✅ 未登录显示 "Connect Wallet"
- ✅ 点击音效按钮可开关通知音
- ✅ 创建新市场时播放音效

---

## 📝 数据更新机制

### 市场数据（公开）
- **触发**: 页面加载时立即获取
- **轮询**: 每 5 秒自动刷新
- **无需登录**: 任何人都能查看

### 用户数据（私有）
- **触发**: 连接钱包后自动加载
- **轮询**: 每 5 秒自动刷新
- **需要登录**: 必须连接钱包

### 音效通知
- **触发**: 检测到市场数量增加
- **条件**: 非首次加载 + 音效开启
- **设置**: 保存在 localStorage

---

## 🎯 与后端的对接

### REST API 端点
```
GET /data/markets               - 获取所有市场
GET /data/market/:id            - 获取单个市场
GET /data/market/:id/orders     - 获取订单
GET /data/market/:id/trades     - 获取成交
GET /data/player/:pid1/:pid2/positions - 获取持仓
GET /data/state                 - 获取全局状态
```

### 命令（通过 zkWASM RPC）
```
REGISTER      - 注册玩家
PLACE_ORDER   - 下单
CANCEL_ORDER  - 撤单
CLAIM         - 提取收益
CREATE_MARKET - 创建市场（Admin）
CLOSE_MARKET  - 关闭市场（Admin）
RESOLVE_MARKET - 解析市场（Admin）
```

---

## 🔑 关键改进点

### 1. 简化架构
- ❌ 不使用 WebSocket（太复杂）
- ✅ 使用简单的 REST + 轮询

### 2. 用户体验
- ✅ 不需要登录也能浏览市场
- ✅ 清晰的登录提示
- ✅ 音效通知新市场
- ✅ 可滚动的市场列表

### 3. 时间显示
- ✅ 统一使用当地时区
- ✅ 英文格式（非中文）
- ✅ 24小时制
- ✅ 格式示例：`Nov 1, 14:30`

### 4. 数据准确性
- ✅ 从后端 API 获取真实数据
- ✅ 正确的精度转换
- ✅ 准确的概率计算

---

## 📚 参考的项目

### Reference 项目
- `reference/frontend-prediction/src/services/api.ts` - API 客户端实现
- `reference/frontend-prediction/src/contexts/MarketContext.tsx` - 数据管理和轮询

### 后端项目
- `backend/socrates-prediction-mkt/ts/src/api.ts` - API 接口定义
- `backend/socrates-prediction-mkt/ts/src/service.ts` - REST 端点
- `backend/socrates-prediction-mkt/ts/src/models.ts` - 数据模型

---

## ✅ 完成状态

### 已完成 ✅
1. ✅ API 客户端创建
2. ✅ 数据类型定义
3. ✅ MarketContext 实现
4. ✅ 市场列表页集成
5. ✅ 市场详情页集成
6. ✅ 左侧边栏集成
7. ✅ 登录检查
8. ✅ 时间本地化
9. ✅ 音效通知系统
10. ✅ 计算工具函数

### 待完成 ⏳
1. ⏳ Portfolio 页面集成
2. ⏳ Wallet 页面集成
3. ⏳ Admin 页面集成
4. ⏳ TradingPanel 下单功能对接
5. ⏳ OrderBook 组件数据对接
6. ⏳ RecentTrades 组件数据对接

---

## 🐛 已修复的问题

1. ✅ 修复 `zkwasm-ts-server` 导入问题（改用 `zkwasm-minirollup-rpc`）
2. ✅ 修复 TypeScript 类型错误
3. ✅ 修复 StatusBadge 组件崩溃
4. ✅ 修复导入路径（`@/` → `../`）
5. ✅ 修复 TradingPanel props 类型不匹配

---

## 🎓 学到的经验

1. **保持简单**: 使用轮询而非复杂的 WebSocket
2. **参考现有项目**: Reference 项目提供了很好的实现模板
3. **类型安全**: TypeScript 类型定义帮助避免错误
4. **用户友好**: 不需要登录也能浏览，降低使用门槛
5. **渐进式增强**: 先实现核心功能，再添加高级特性

---

最后更新: 2025-11-01

